# FIXME : There are problems with conditional build of switchvt

noinst_PROGRAMS = r2c_raw

r2c_raw_SOURCES = r2c_raw.c

switchvt_SOURCES = switchvt.c

EXTRA_DIST = Makefile.purec $(r2c_raw_SOURCES) $(switchvt_SOURCES)
